# SSS (Codex) — arşiv

Mobil SSS kaldırıldı: `SssMobile` ve `scs-mobile.css` dosyası silindi.

Güncel kod: `src/components/sss/` altında `SssRouter`, `SssDesktop`, `faqs`; Next route `app/sss/` altında `page.tsx` ve `scs-desktop.css` import’u.

Eski mobil turuncu override rehberi geçersizdir.
