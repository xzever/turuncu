"use client";

/**
 * IletisimDesktop — Faz 04 wrapper.
 * Desktop (≥1280 + pointer:fine) için mevcut `IletisimClient` render edilir.
 * Faz 10'da içerik buraya taşınacak; mobile branch temizlenecek.
 */
export { default } from "../../../app/iletisim/IletisimClient";
