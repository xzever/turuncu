"use client";

/**
 * SistemlerimizDesktop — Faz 05 wrapper.
 * Desktop (≥1280 + pointer:fine) için mevcut `SistemlerimizClient` render edilir.
 */
export { default } from "../../../app/sistemlerimiz/SistemlerimizClient";
